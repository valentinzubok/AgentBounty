# AgentBounty

GenLayer Intelligent Contract — task bounty with **frozen delivery URL** + LLM `pay_worker` consensus.

**IC-only repo** (no wallet app). Console: [AgentBountyDesk](../AgentBountyDesk).

```bash
python3 -m pytest -q
```

Studio: paste `contracts/AgentBounty.py`. See `SUBMIT.md`.
